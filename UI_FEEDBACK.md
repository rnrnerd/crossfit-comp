# UI-правки (накапливаем, в работу — по команде)

## 1. Лидерборд — вид как на референс-скриншоте

**Навигация сверху** (горизонтальные табы с прокруткой, НЕ нижнее меню):
- вкладки: `…вки` · `Расписание` (с бейджем-счётчиком, напр. `21`) · **`Таблица лидеров`** (активная — оранжевый текст + оранжевое подчёркивание) · `Shop`

**Строка фильтров** под табами (горизонтальный скролл):
- круглая кнопка с лупой (поиск)
- пилюля-фильтр: цветной бейдж (напр. розовый `INDY`) + текст (`Rx Men`) + `…`
- ещё пилюля (напр. `В общем зачёте`)

**Заголовок секции:** капсом, жирный, **на русском** (напр. `ОФИЦИАЛЬНАЯ ТАБЛИЦА ЛИДЕРОВ` / `ТАБЛИЦА ЛИДЕРОВ`). Весь текст в интерфейсе — на русском.

**Строки = карточки** (скруглённые, с зазором между собой):
- слева — **место**: топ-3 = медаль (🥇🥈🥉) с цифрой и ленточкой; с 4-го = просто номер
- круглая **аватарка** атлета
- **имя** капсом, жирное
- НЕТ строки с флагом страны и командой/клубом (убрать)
- справа — **число (очки)** жирным + шеврон `⌄` (раскрытие строки)
- у **1-го места** — выделенная рамка-обводка

**Цвета:** оставить как сейчас сделано (текущая тёмная тема + золотой акцент). Палитру со скриншота НЕ копировать — только структуру/раскладку.

**Язык:** весь интерфейс на русском.

## 2. Раздел «WOD'ы» → переименовать в «Комплексы»

- Переименовать раздел/вкладку `WOD'ы` → **`Комплексы`** (везде: навигация, заголовки).
- Вверху раздела — **фильтр по категории** (Новички / Любители / Продвинутые) **и по гендеру** (М / Ж).
- У каждого комплекса — **раскрывающийся список** (аккордеон): по тапу разворачивается описание комплекса.

## 3. Экран отдельного захода (как на референс-скриншоте)

Структуру берём со скрина, **цвета — наши** (тёмная тема + золотой акцент, без радужного градиента со скрина).

Сверху вниз:
1. Стрелка «назад» слева вверху.
2. **Карточка «РАЗМИНКА»** (WARM-UP): диапазон времени с линией-стрелкой — `17:11 ——→ 17:51`. На скрине цветной градиент → у нас заменить на наш акцент (золотой / акцентная заливка).
3. **Карточка «БРИФИНГ»** (BRIEFING): так же `17:51 ——→ 18:16`, тёмная карточка с обводкой.
4. **Карточка «ЗАХОД НАЧИНАЕТСЯ В»** (HEAT STARTS AT): по центру подпись + крупное время `18:16`.
5. Заголовок **`СТАРТ-ЛИСТ`** (START LIST), капсом.
6. **Список атлетов** — карточки:
   - слева **номер** (дорожка/позиция),
   - **имя** капсом, жирное,
   - флаг страны (если будут данные о стране — иначе убрать),
   - цветная точка + **категория** (на скрине `Rx Men`; у нас — наша категория, напр. «Продвинутые М»).

Весь текст — на русском.
